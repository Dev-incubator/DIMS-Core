@model DIMS_Core.Models.ProgressModels.CreateEditNoteViewModel;
@{
    ViewData["Title"] = "EditNoteWindowView";
}

<form asp-action="EditNote" asp-controller="Progress" method="post">
    <div>
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Task Track</h5>
            </div>
            <div class="modal-body">
                <div>
                    <input type="hidden" asp-for="UserTaskId" />
                    <label>Date</label>
                    <input type="date" asp-for="TrackDate" asp-format="{0:yyyy-MM-dd}" readonly />
                </div>
                <div>
                    <label>Note</label>
                    <textarea asp-for="TrackNote">@Model.TrackNote</textarea>
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary">Save</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Back to grid</button>
            </div>
        </div>
    </div>
</form>
