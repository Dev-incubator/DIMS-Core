@model SampleViewModel

@{
    ViewData["Title"] = "Delete";
    var controller = "samples";
}

<h1>Delete</h1>

<div>
    <h4>SampleModel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>

<h3>Are you sure you want to delete this task track?</h3>

@await Html.PartialAsync("_BottomDelete", new BottomButtonsViewModel()
{
    Id = Model.SampleId,
    Controller = controller,
    BackAction = ViewBag.BackAction,
    BackController = ViewBag.BackController ??= controller
}) new BackToListViewModel { Controller = "samples" })